# IDEs
.idea/
.vscode/
*.swp
*.swo

# project
notes/
zero/
*.rar
*.tar
*.zip
*.7z

# project volumes
docker_volume/

#.envs files
.env
.env.*
!.env.*.template

# Terraform local cache and downloaded provider binaries (plugins)
.terraform/
# Terraform state files and backups (live infrastructure metadata and secrets)
*.tfstate
*.tfstate.*
# Terraform variable files (passwords, API tokens, and IPs)
*.tfvars
*.tfvars.json
!*.tfvars.template
# Terraform plan files (contain binary execution plan details)
tfplan
*plan